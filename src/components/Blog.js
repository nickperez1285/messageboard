import "./Blog.css";

/* Blog component to display some content at the page top. */
let Blog = () => {
    return (
        <main>
            <header className='main-header'>
                <h1>Encypted Message Board</h1>
                <p>A simple encrypted message board using React and Firebase.</p>
            </header>
            <article>
            </article>
        </main>
    )
};

export default Blog;